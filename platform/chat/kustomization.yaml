apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: inference
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: chat-html
    files:
      - index.html
  - name: chat-nginx
    files:
      - default.conf=nginx.conf
