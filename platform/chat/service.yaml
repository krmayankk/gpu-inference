apiVersion: v1
kind: Service
metadata:
  name: chat
  labels:
    app: chat
    app.kubernetes.io/part-of: gpu-inference
spec:
  selector:
    app: chat
  ports:
    - name: http
      port: 80
      targetPort: http
